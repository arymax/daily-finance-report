# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# 本地環境（API keys 等不進 repo）
.env
*.env

# 看板 API key（本地生成，不進 repo）
docs/config.js

# 執行日誌（本地就好，不需要同步）
logs/

# 執行鎖（runtime artifact，不應進 repo）
main.lock

# Windows 暫存
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/

# Serena MCP（本機絕對路徑設定，不進 repo）
.serena/

# uv — uv.lock 與 .python-version 需被追蹤
# uv.lock       ← 精確版本鎖定，請 commit
# .python-version ← Python 版本聲明，請 commit

# thesis/ — 個股買入背景，應被 git 追蹤（記錄個人投資邏輯）
# thesis/{TICKER}.md ← 每次更新投資邏輯後請 commit

# 確保以下被 git 追蹤（不加 !，只是說明）
# reports/      ← 每日報告，需同步
# memory/       ← 每日記憶摘要，需同步
# portfolio.json ← 持倉資料，需同步
